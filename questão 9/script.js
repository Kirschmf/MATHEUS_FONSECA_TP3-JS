const parOuImpar = (num) => {
    return num % 2 === 0 ? "par" : "impar";
  }
  
  const resA = parOuImpar(2);
  console.log(resA); // Mostra a string "par" no console.
  
  const resB = parOuImpar(3);
  console.log(resB); // Mostra a string "impar" no console.